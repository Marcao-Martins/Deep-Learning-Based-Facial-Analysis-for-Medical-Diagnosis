torch
torchvision
Pillow
numpy
mlflow
scikit-learn
tqdm
# matplotlib and seaborn are not strictly needed by the pipeline itself if plots are only in my_mlflow_utils
# but often useful for local debugging or direct use in notebooks.
# Add them if you anticipate needing them directly in this ResNet module context.
# matplotlib
# seaborn 